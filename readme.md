# ReplyPilot Marketing Website

This is the landing page for ReplyPilot.ai.

## Tech Stack
- **HTML5**
- **Tailwind CSS** (via CDN for simplicity)
- **FontAwesome** (for icons)

## How to Run Locally
1. Open `index.html` in your browser.
   OR
2. Run a local server:
   ```bash
   python3 -m http.server 8080
   ```
   Then visit `http://localhost:8080`

## Deployment
Since this is a static site (just HTML/CSS), you can deploy it for free on:
- **Vercel** (Drag and drop the folder)
- **Netlify** (Drag and drop the folder)
- **GitHub Pages** (Push to a repo and enable Pages)

## To-Do Before Launch
1. **Screenshots:** Replace the placeholder `div` in the Hero section with a real screenshot of the extension in action.
2. **Links:** Update the "Get Chrome Extension" buttons with the actual Chrome Web Store link once published.
3. **Legal:** Add actual text for Privacy Policy and Terms of Service (required for Chrome Store).
